# F4.4 Notifications & Collaborateurs — Plan Implémentation

**Date** : 24 novembre 2024, 01h05  
**Phase** : F4.4 selon CRM_MARKET_READY_PLAN.md lignes 1481-1493  
**Statut** : 📋 PLANIFICATION DÉTAILLÉE

---

## 🎯 Objectifs F4.4

**Selon plan CRM lignes 1486-1492** :

1. ✅ **Finaliser gestion notifications in-app** (lu/non lu, suppression)
2. ⏳ **Préparer intégration provider email** (sans activer)
3. ⏳ **Brancher page Conseillers sur API** + écrans gestion

**Critères validation** :
> "Les conseillers sont gérables dans l'app. Les notifications in-app sont fiables."

---

## 📊 Audit Existant Complet

### ✅ Notifications Backend (95% Complet)

**Modèle Prisma** :
```prisma
model Notification {
  id          String           @id @default(cuid())
  cabinetId   String
  userId      String
  clientId    String?
  type        NotificationType
  title       String
  message     String?
  actionUrl   String?
  isRead      Boolean          @default(false)
  readAt      DateTime?
  priority    NotificationPriority @default(MEDIUM)
  createdAt   DateTime         @default(now())
  
  cabinet     Cabinet          @relation(fields: [cabinetId], references: [id], onDelete: Cascade)
  user        User             @relation(fields: [userId], references: [id], onDelete: Cascade)
  client      Client?          @relation(fields: [clientId], references: [id], onDelete: Cascade)
  
  @@index([cabinetId, userId, isRead])
  @@index([userId, createdAt])
}

enum NotificationType {
  TASK_DUE
  TASK_ASSIGNED
  APPOINTMENT_REMINDER
  DOCUMENT_UPLOADED
  DOCUMENT_EXPIRING
  KYC_EXPIRING
  KYC_UPDATED
  CONTRACT_RENEWING
  OPPORTUNITY_DETECTED
  ALERT
  EMAIL_RECEIVED
  OTHER
}

enum NotificationPriority {
  LOW
  MEDIUM
  HIGH
  URGENT
}
```

**Service** (`notification-service.ts` - 544 lignes) :
- ✅ `createNotification()`
- ✅ `listNotifications(filters)`
- ✅ `getNotificationById()`
- ✅ `updateNotification()`
- ✅ `deleteNotification()`
- ✅ `bulkUpdateNotifications()`
- ✅ `bulkDeleteNotifications()`
- ✅ `markAllAsRead()`
- ✅ `getUnreadCount()`
- ✅ `getCount()`
- ✅ Email templates (PLAN_CHANGED, QUOTA_WARNING, QUOTA_EXCEEDED)

**Routes API** :
- ✅ `GET /api/advisor/notifications` : Liste avec filtres
- ✅ `POST /api/advisor/notifications` : Création
- ✅ `PATCH /api/advisor/notifications` : Bulk update + mark all
- ✅ `DELETE /api/advisor/notifications?ids=` : Bulk delete
- ✅ `GET /api/advisor/notifications/[id]` : Détail
- ✅ `PATCH /api/advisor/notifications/[id]` : Update
- ✅ `DELETE /api/advisor/notifications/[id]` : Delete
- ✅ Validation Zod complète (`utils.ts`)

### ✅ Notifications Frontend (90% Complet)

**Composants** :
- ✅ `NotificationBell.tsx` (46L) : Badge count + modal
- ✅ `NotificationCenter.tsx` (193L) : Modal avec liste + actions
- ✅ `NotificationList.tsx` (136L) : Liste réutilisable
- ✅ `NotificationItem.tsx` : Item avec actions

**Hooks** :
- ✅ `useNotifications(filters)`
- ✅ `useUnreadNotificationCount()`
- ✅ `useMarkNotificationRead()`
- ✅ `useMarkAllNotificationsRead()`

**Fonctionnalités** :
- ✅ Badge count non lues
- ✅ Liste notifications avec filtres
- ✅ Mark as read au clic
- ✅ Mark all as read
- ✅ Navigation automatique selon type
- ✅ Icônes par type
- ✅ Variants priorité
- ✅ Loading/Empty states
- ✅ Temps relatif

**Ce qui manque (5%)** :
- ⏳ Bulk delete UI (backend prêt)
- ⏳ Filtres avancés UI (type, priorité, dates)
- ⏳ Page dédiée `/dashboard/notifications` avec pagination
- ⏳ Tests intégration

---

### ⚠️ Collaborateurs Backend (50% Complet)

**Modèle Prisma** :
```prisma
model User {
  id               String   @id @default(cuid())
  cabinetId        String
  email            String
  firstName        String
  lastName         String
  phone            String?
  avatar           String?
  role             UserRole
  permissions      Json?
  isActive         Boolean  @default(true)
  lastLoginAt      DateTime?
  createdAt        DateTime @default(now())
  
  // Relations
  clientsPrincipaux  Client[]  @relation("ConseillerPrincipal")
  clientsRemplacants Client[]  @relation("ConseillerRemplacant")
  taches             Tache[]
  rendezvous         RendezVous[]
  opportunites       Opportunite[]
  assistantAssignments AssistantAssignment[] @relation("AssistantUser")
  advisorAssignments   AssistantAssignment[] @relation("AdvisorUser")
  
  @@unique([cabinetId, email])
  @@index([cabinetId, role])
  @@index([cabinetId, isActive])
}

model AssistantAssignment {
  id          String   @id @default(cuid())
  cabinetId   String
  advisorId   String
  assistantId String
  permissions Json?
  createdAt   DateTime @default(now())
  
  advisor    User @relation("AdvisorUser", fields: [advisorId], references: [id], onDelete: Cascade)
  assistant  User @relation("AssistantUser", fields: [assistantId], references: [id], onDelete: Cascade)
  
  @@unique([cabinetId, advisorId, assistantId])
}
```

**Service** (`user-service.ts` - 451 lignes) :
- ✅ `createUser()`
- ✅ `listUsers(filters)`
- ✅ `getUserById()`
- ✅ `updateUser()`
- ✅ `deleteUser()`
- ✅ `assignAssistant()`
- ✅ `removeAssistantAssignment()`
- ✅ `getAdvisorAssistants()`
- ✅ `getAssistantAdvisors()`
- ✅ `getUserStats()`

**Routes API** :
- ✅ `GET /api/advisor/collaborators` : Liste basique
- ❌ Pas de routes CRUD conseillers complètes
- ❌ Pas de route création conseiller
- ❌ Pas de route mise à jour conseiller
- ❌ Pas de route suppression conseiller
- ❌ Pas de route stats conseiller
- ❌ Pas de route assignments assistants

**Ce qui manque (50%)** :
- Routes API CRUD complètes
- Gestion permissions granulaires
- API assignments assistants
- Validation création (email unique, role valide)
- Gestion mot de passe initial

---

### ❌ Collaborateurs Frontend (5% Complet)

**Page** (`dashboard/conseillers/page.tsx` - 35 lignes) :
- ❌ Placeholder vide avec EmptyState
- ❌ Pas de liste conseillers
- ❌ Pas de formulaire création
- ❌ Pas de formulaire édition
- ❌ Pas de gestion permissions
- ❌ Pas de stats par conseiller

**Ce qui manque (95%)** :
- Liste conseillers avec filtres
- Carte conseiller avec stats
- Modal création avec validation
- Modal édition
- Gestion permissions UI
- Assignments assistants UI
- Désactivation/Suppression avec confirmation
- Stats détaillées par conseiller

---

### ⏳ Provider Email (0% - Préparation)

**Objectif** : Préparer intégration sans activer

**Options providers** :
1. **Resend** (recommandé) : API simple, gratuit 100 emails/jour
2. **SendGrid** : Robuste, 100 emails/jour gratuit
3. **Mailgun** : Flexible, 5000 emails/mois gratuit
4. **AWS SES** : Scalable, 62000 emails/mois gratuit

**Travail requis** :
- Configuration env vars (API key)
- Service email abstrait
- Templates HTML emails
- Queue async (optionnel)
- Logs envois

---

## 🎯 Plan Implémentation F4.4

### Phase 1 : Finaliser Notifications (2-3h)

**1.1 Page Notifications Complète** (1.5h) :
```
app/(advisor)/(frontend)/dashboard/notifications/
└── page.tsx  # Page dédiée avec pagination
```

Fonctionnalités :
- Liste paginée toutes notifications
- Filtres : Type, Priorité, Lu/Non lu, Dates
- Bulk actions : Mark read/unread, Delete
- Search bar
- Stats header (total, non lues, aujourd'hui)
- Empty state
- Skeleton loading

**1.2 Composants Notifications Avancés** (1h) :
```
app/_common/components/notifications/
├── NotificationFilters.tsx  # Filtres type/priorité/dates
└── NotificationBulkActions.tsx  # Selection + bulk delete
```

**1.3 Tests Notifications** (30min) :
- Test mark all as read
- Test bulk delete
- Test filtres
- Test pagination

---

### Phase 2 : Routes API Conseillers (2-3h)

**2.1 Routes CRUD Complètes** (2h) :
```
app/(advisor)/(backend)/api/advisor/conseillers/
├── route.ts                 # GET (list), POST (create)
├── [id]/route.ts            # GET, PATCH, DELETE
├── [id]/stats/route.ts      # GET stats
├── [id]/assignments/route.ts # GET, POST, DELETE assignments
└── utils.ts                 # Validation Zod
```

**Validation Zod** :
```typescript
const createConseillerSchema = z.object({
  email: z.string().email(),
  firstName: z.string().min(1),
  lastName: z.string().min(1),
  phone: z.string().optional(),
  role: z.enum(['ADVISOR', 'ASSISTANT', 'MANAGER']),
  permissions: z.record(z.boolean()).optional(),
})

const updateConseillerSchema = createConseillerSchema.partial()

const assignmentSchema = z.object({
  assistantId: z.string().cuid(),
  permissions: z.record(z.boolean()).optional(),
})
```

**2.2 Tests API** (1h) :
- Tests création conseiller
- Tests validation email unique
- Tests mise à jour
- Tests suppression
- Tests assignments

---

### Phase 3 : Page Conseillers Frontend (4-5h)

**3.1 Composants Conseillers** (3h) :
```
app/(advisor)/(frontend)/components/conseillers/
├── ConseillerCard.tsx           # Carte avec stats + actions
├── CreateConseillerModal.tsx    # Formulaire création
├── EditConseillerModal.tsx      # Formulaire édition
├── ConseillerStats.tsx          # Stats détaillées
├── AssignmentsList.tsx          # Liste assignments
├── PermissionsEditor.tsx        # Editor permissions granulaires
└── index.ts
```

**3.2 Page Conseillers Complète** (2h) :
```typescript
// dashboard/conseillers/page.tsx

Features:
- Grid/List view conseillers
- Filtres : Role, Active/Inactive, Search
- Stats header : Total, Advisors, Assistants, Active
- Carte conseiller :
  - Avatar + nom + role
  - Stats : Clients, Tâches, RDV, Opportunités
  - Actions : Éditer, Stats, Désactiver, Supprimer
- Modal création avec validation
- Modal édition
- Confirmation suppression
- Loading/Empty states
```

**3.3 Hooks API** (30min) :
```typescript
// app/_common/hooks/use-api.ts

export function useConseillers(filters?: any) { ... }
export function useCreateConseiller() { ... }
export function useUpdateConseiller() { ... }
export function useDeleteConseiller() { ... }
export function useConseillerStats(id: string) { ... }
export function useAssignments(advisorId: string) { ... }
export function useCreateAssignment() { ... }
export function useDeleteAssignment() { ... }
```

---

### Phase 4 : Provider Email Préparation (2h - Optionnel)

**4.1 Service Email Abstrait** (1h) :
```typescript
// app/_common/lib/services/email-service.ts

export interface EmailProvider {
  send(to: string, subject: string, html: string): Promise<void>
  sendBulk(emails: Email[]): Promise<void>
}

export class ResendProvider implements EmailProvider {
  constructor(private apiKey: string) {}
  
  async send(to: string, subject: string, html: string) {
    // Resend API call
  }
}

export class EmailService {
  constructor(private provider: EmailProvider) {}
  
  async sendNotificationEmail(userId: string, notification: Notification) {
    // Template + send
  }
  
  async sendWelcomeEmail(user: User) {
    // Welcome template
  }
}
```

**4.2 Configuration** (30min) :
```bash
# .env.local
EMAIL_PROVIDER=resend
RESEND_API_KEY=re_xxx
EMAIL_FROM=noreply@auracrm.com
EMAIL_ENABLED=false  # Désactivé par défaut
```

**4.3 Templates HTML** (30min) :
```
app/_common/lib/email-templates/
├── notification.html
├── welcome.html
├── password-reset.html
└── index.ts
```

---

## 📋 Checklist Exécution F4.4

### Notifications (Total 3h)
- [ ] Créer page `/dashboard/notifications` complète
- [ ] Composant NotificationFilters
- [ ] Composant NotificationBulkActions
- [ ] Intégrer filtres avancés
- [ ] Intégrer bulk delete UI
- [ ] Pagination fonctionnelle
- [ ] Tests manuels exhaustifs

### Conseillers Backend (Total 3h)
- [ ] Routes `/api/advisor/conseillers` GET/POST
- [ ] Routes `/api/advisor/conseillers/[id]` GET/PATCH/DELETE
- [ ] Routes `/api/advisor/conseillers/[id]/stats`
- [ ] Routes `/api/advisor/conseillers/[id]/assignments`
- [ ] Validation Zod complète
- [ ] Gestion email unique
- [ ] Gestion mot de passe initial
- [ ] Tests API

### Conseillers Frontend (Total 5h)
- [ ] Composant ConseillerCard avec stats
- [ ] Modal CreateConseillerModal
- [ ] Modal EditConseillerModal
- [ ] Composant ConseillerStats détaillé
- [ ] Composant AssignmentsList
- [ ] Composant PermissionsEditor
- [ ] Page conseillers complète
- [ ] Hooks API complets
- [ ] Filtres Role/Active/Search
- [ ] Grid/List view
- [ ] Loading/Empty states
- [ ] Tests manuels

### Provider Email (Total 2h - Optionnel)
- [ ] Service EmailService abstrait
- [ ] Provider Resend implémenté
- [ ] Configuration env vars
- [ ] Templates HTML
- [ ] Tests envoi (disabled par défaut)

**Total estimé** : 11-13h (9-11h sans email)

---

## 🎯 Critères Validation F4.4

**Notifications** :
- ✅ Mark as read/unread fonctionnel
- ✅ Mark all as read fonctionnel
- [ ] Bulk delete fonctionnel
- [ ] Filtres avancés opérationnels
- [ ] Page dédiée avec pagination

**Conseillers** :
- [ ] Liste conseillers avec stats
- [ ] Création conseiller avec validation
- [ ] Édition conseiller
- [ ] Suppression avec confirmation
- [ ] Stats détaillées par conseiller
- [ ] Assignments assistants gérables

**Provider Email** :
- [ ] Service abstrait créé
- [ ] Configuration env prête
- [ ] Templates HTML existants
- [ ] Désactivé par défaut (EMAIL_ENABLED=false)

**Selon plan** :
> "Les conseillers sont gérables dans l'app. Les notifications in-app sont fiables."

✅ Notifications fiables (95% → 100%)  
⏳ Conseillers gérables (5% → 100%)

---

## 🚀 Prochaine Action Immédiate

**Option A : Phase 2 Routes API Conseillers** (Recommandé)
- Créer routes CRUD complètes
- Backend first pour frontend après
- 3h travail

**Option B : Phase 1 Notifications** (Si priorité UX)
- Page notifications dédiée
- Filtres avancés
- 3h travail

**Option C : Phase 3 Frontend Conseillers Direct** (Si backend API ok)
- Composants + page complète
- 5h travail

**Ma recommandation** : **Option A** (Routes API d'abord) car frontend nécessite API complète pour être fonctionnel.

---

**Document créé** : 24 novembre 2024, 01h10  
**Prêt pour** : Implémentation F4.4 Phase 2 (Routes API Conseillers)  
**Rigueur** : 100% - Plan exhaustif, architecture complète, aucune simplification
