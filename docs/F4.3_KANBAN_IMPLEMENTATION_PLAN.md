# F4.3 Tâches & Opportunités Kanban — Plan d'Implémentation

**Date** : 24 novembre 2024, 00h30  
**Phase** : F4.3 selon CRM_MARKET_READY_PLAN.md lignes 1469-1479  
**Statut** : 📋 PLANIFICATION DÉTAILLÉE

---

## 🎯 Objectifs F4.3

Selon plan CRM :
1. ✅ **Mettre en place des vues Kanban** pour tâches et opportunités
2. ✅ **Ajouter des métriques** (tâches en retard, valeur pipe, etc.)
3. ✅ **Lier objectifs ↔ simulateurs** et projets ↔ facturation

**Critère validation** :
> "L'équipe peut suivre son pipe commercial et ses tâches sans outils externes."

---

## 📊 Analyse Existant (Audit Complet)

### ✅ Backend Tâches (100% Production-Ready)

**Modèle Prisma** (`schema.prisma` lignes 961-992) :
```prisma
model Tache {
  id           String        @id @default(cuid())
  cabinetId    String
  assignedToId String
  clientId     String?
  projetId     String?
  createdById  String
  title        String
  description  String?
  type         TacheType     // CALL, EMAIL, MEETING, DOCUMENT, FOLLOW_UP, OTHER
  priority     TachePriority // LOW, MEDIUM, HIGH, URGENT
  status       TacheStatus   // TODO, IN_PROGRESS, COMPLETED, CANCELLED
  dueDate      DateTime?
  completedAt  DateTime?
  reminderDate DateTime?
  createdAt    DateTime
  updatedAt    DateTime
  
  documents    TacheDocument[]
  
  @@index([cabinetId, status, dueDate])
}
```

**Service** (`app/_common/lib/services/tache-service.ts` - 546 lignes) :
- ✅ 12 méthodes complètes
- ✅ `createTache()`, `getTaches()`, `updateTache()`, `deleteTache()`
- ✅ `completeTache()`, `reassignTache()`
- ✅ `getMyTaches()`, `getOverdueTaches()`, `getTachesWithReminderToday()`
- ✅ `getUserStatistics()`, `getStatistics()`
- ✅ Filtres : assignedTo, client, projet, type, status, priority, dueBefore, search
- ✅ Timeline events automatiques
- ✅ Validation existence user/client/projet

**Routes API** (`app/(advisor)/(backend)/api/advisor/taches/`) :
- ✅ `GET /api/advisor/taches` : Liste avec filtres
- ✅ `POST /api/advisor/taches` : Création
- ✅ `GET /api/advisor/taches/[id]` : Détail
- ✅ `PATCH /api/advisor/taches/[id]` : Mise à jour
- ✅ `DELETE /api/advisor/taches/[id]` : Suppression
- ✅ Validation Zod complète (`utils.ts`)

### ✅ Backend Opportunités (100% Production-Ready)

**Modèle Prisma** (analysé précédemment) :
- ✅ Opportunite avec tous champs nécessaires
- ✅ Relations client, conseiller, projet
- ✅ Enum OpportuniteType, OpportuniteStatus, OpportunitePriority

**Service** (`app/_common/lib/services/opportunite-service.ts` - 650 lignes) :
- ✅ CRUD complet
- ✅ Méthodes statistiques et filtres avancés
- ✅ Conversion opportunité → projet
- ✅ Timeline events automatiques

**Routes API** (`app/(advisor)/(backend)/api/advisor/opportunites/`) :
- ✅ GET, POST, PATCH, DELETE complets
- ✅ `GET /opportunites/pipeline` : Données pipeline
- ✅ `POST /opportunites/[id]/convert` : Conversion projet
- ✅ Validation Zod stricte

### ⚠️ Frontend Tâches (Existant mais Incomplet)

**Page** (`app/(advisor)/(frontend)/dashboard/taches/page.tsx`) :
- ⚠️ **Existe mais vue Liste uniquement**
- ❌ **Pas de vue Kanban drag & drop**
- ✅ Filtres probablement présents
- ✅ Création/édition probables

**Ce qui manque** :
- Vue Kanban avec colonnes TODO | IN_PROGRESS | COMPLETED
- Drag & drop pour changer statut
- Réassignation par drag & drop
- Métriques avancées (tâches en retard, par priorité, etc.)
- Bulk actions

### ⚠️ Frontend Opportunités (Existant mais Incomplet)

**Page** (`app/(advisor)/(frontend)/dashboard/opportunites/page.tsx` - 857 lignes analysée) :
- ✅ Vue Pipeline statique (colonnes par statut)
- ✅ Vue Liste
- ✅ Statistiques basiques (4 cartes)
- ✅ Filtres type/priorité/search
- ✅ Conversion en projet

**Ce qui manque** :
- ❌ **Drag & drop entre colonnes pipeline** (actuellement statique)
- Métriques avancées consolidées
- Graphiques évolution valeur pipe
- Taux conversion par type
- Prévisions

---

## 🎯 Travail à Réaliser F4.3

### Phase 1 : Vue Kanban Tâches (Priorité HAUTE)

**Objectif** : Tableau Kanban drag & drop pour gérer les tâches visuellement

**Composants à créer** :
1. `KanbanBoard.tsx` : Conteneur principal Kanban réutilisable
2. `KanbanColumn.tsx` : Colonne Kanban avec header/count
3. `KanbanCard.tsx` : Carte tâche draggable
4. `TaskQuickEdit.tsx` : Modal édition rapide

**Bibliothèque drag & drop** :
- **Option 1** : `@dnd-kit/core` (recommandé - moderne, accessible, performant)
- **Option 2** : `react-beautiful-dnd` (classique mais plus maintenu)
- **Choix** : `@dnd-kit/core` pour F4.3

**Fonctionnalités Kanban Tâches** :
- ✅ 3-4 colonnes : TODO | IN_PROGRESS | COMPLETED (+ optionnel CANCELLED)
- ✅ Drag & drop pour changer statut avec animation fluide
- ✅ Compteur tâches par colonne
- ✅ Filtres : Assigné à, Priorité, Type, Date
- ✅ Tri : Date échéance, Priorité, Date création
- ✅ Badge priorité (couleurs : LOW gris, MEDIUM bleu, HIGH orange, URGENT rouge)
- ✅ Badge type (icônes : CALL phone, EMAIL envelope, MEETING calendar, etc.)
- ✅ Indicateur retard (rouge si dueDate < aujourd'hui et status ≠ COMPLETED)
- ✅ Avatar assigné
- ✅ Lien client (si tâche liée)
- ✅ Quick actions : Éditer, Réassigner, Compléter, Supprimer
- ✅ Bulk selection (multi-select avec Ctrl/Cmd+clic)
- ✅ Bulk actions : Réassigner, Changer priorité, Supprimer

**Intégration API** :
```typescript
// GET /api/advisor/taches?status=TODO pour chaque colonne
// PATCH /api/advisor/taches/[id] { status: 'IN_PROGRESS' } lors drag & drop
```

**Métriques Tâches** (cartes stats) :
1. **Tâches en retard** : Count + pourcentage + évolution
2. **Tâches du jour** : dueDate = aujourd'hui
3. **Taux complétion** : completed / total × 100
4. **Charge par conseiller** : Graphique barres assignedTo

---

### Phase 2 : Vue Kanban Opportunités (Priorité HAUTE)

**Objectif** : Transformer pipeline statique en Kanban drag & drop

**Colonnes Pipeline** (selon `pipelineStages` existant) :
- DETECTED (Détectée)
- CONTACTED (Contactée)
- QUALIFIED (Qualifiée)
- PROPOSAL_SENT (Proposition envoyée)
- NEGOTIATION (Négociation)
- WON (Gagnée)
- LOST (Perdue)

**Améliorations vs Existant** :
- ✅ Drag & drop entre colonnes avec changement automatique statut
- ✅ Animation smooth lors drag
- ✅ Double-clic carte → Modal détail
- ✅ Indication confiance % avec barre de progression
- ✅ Somme valeurs estimées par colonne (déjà présent, à conserver)
- ✅ Quick actions : Éditer, Convertir en projet, Supprimer
- ✅ Historique changements statut (timeline)

**Métriques Opportunités Avancées** :
1. **Valeur pipeline totale** : Somme estimatedValue × probability
2. **Taux conversion global** : WON / (WON + LOST) × 100
3. **Taux conversion par type** : Graphique barres
4. **Durée moyenne par étape** : Temps moyen DETECTED → WON
5. **Top 3 conseillers** : Par valeur gagnée
6. **Évolution mensuelle** : Graphique ligne valeur pipeline

**API Extensions Nécessaires** :
```typescript
// Nouvelle route : GET /api/advisor/opportunites/metrics
// Retourne : { totalValue, weightedValue, conversionRate, avgDuration, byType, byConseiller }
```

---

### Phase 3 : Métriques Consolidées (Priorité MOYENNE)

**Dashboard Global Tâches + Opportunités** :

**Composant** : `DashboardMetrics.tsx`

**Cartes métriques** (8 cartes 2x4 grid) :
1. **Tâches actives** : TODO + IN_PROGRESS (badge urgent si > 10 en retard)
2. **Tâches en retard** : Count + % + trend 7j
3. **Opportunités actives** : DETECTED → NEGOTIATION
4. **Valeur pipeline** : Somme pondérée + trend
5. **Taux conversion** : % + comparaison mois précédent
6. **Tâches complétées semaine** : Count + graphique sparkline
7. **Opportunités gagnées mois** : Count + valeur totale
8. **Charge équipe** : Tâches par conseiller + graphique

**Graphiques** :
- **Graphique ligne** : Évolution valeur pipeline 6 derniers mois
- **Graphique barres** : Tâches par type (derniers 30j)
- **Graphique donut** : Répartition opportunités par statut
- **Heatmap** : Tâches complétées par jour (style GitHub)

**Bibliothèque charts** :
- **Choix** : `recharts` (React-friendly, performant, customizable)
- Alternative : `chart.js` via `react-chartjs-2`

---

### Phase 4 : Liens Objectifs & Projets (Priorité BASSE)

**Lien Objectifs ↔ Simulateurs** :

**Contexte** : Actuellement aucun modèle Objectif n'existe dans Prisma.

**Actions** :
1. Créer modèle `Objectif` dans Prisma :
```prisma
model Objectif {
  id              String   @id @default(cuid())
  cabinetId       String
  conseillerId    String?  // Si objectif personnel
  type            ObjectifType  // REVENUE, CLIENT_COUNT, AUM, CONVERSION_RATE
  target          Decimal
  period          String   // "2024-Q1", "2024", etc.
  relatedSimulators String[] // Array IDs simulateurs
  
  @@index([cabinetId, conseillerId, period])
}
```

2. Service `ObjectifService` avec méthodes tracking
3. Routes API `/api/advisor/objectifs`
4. Widget dashboard "Progression objectifs"
5. **Lien simulateur** : Incrémenter compteur quand simulateur converti en client

**Lien Projets ↔ Facturation** :

**Contexte** : Modèle Projet existe, modèle Invoice à créer (F5.1).

**Actions différées à F5.1** :
- Créer modèle `Invoice` avec `projetId`
- Lors création facture, lier automatiquement au projet
- Widget "Facturation projet" dans détail projet
- Champ `Projet.totalFacture` calculé

---

## 🛠️ Stack Technique F4.3

### Bibliothèques Nouvelles

```json
{
  "dependencies": {
    "@dnd-kit/core": "^6.1.0",
    "@dnd-kit/sortable": "^8.0.0",
    "@dnd-kit/utilities": "^3.2.2",
    "recharts": "^2.10.0"
  }
}
```

### Composants UI Réutilisables

**Nouveaux composants** :
- `KanbanBoard` : Container drag & drop
- `KanbanColumn` : Colonne avec header
- `KanbanCard` : Carte draggable générique
- `MetricCard` : Carte métrique avec icône/valeur/trend
- `TrendIndicator` : Flèche ↑↓ avec pourcentage
- `SparklineChart` : Graphique mini-ligne
- `ProgressRing` : Cercle de progression
- `BulkActionsBar` : Barre actions multiples
- `QuickEditModal` : Modal édition rapide

**Existants à réutiliser** :
- `Badge`, `Button`, `Card`, `Dialog`, `Select`, `Input`
- `LoadingState`, `ErrorState`, `EmptyState`

---

## 📐 Architecture Composants Kanban

### Structure Fichiers

```
app/(advisor)/(frontend)/
├── components/
│   ├── kanban/
│   │   ├── KanbanBoard.tsx         # Container principal
│   │   ├── KanbanColumn.tsx        # Colonne draggable zone
│   │   ├── KanbanCard.tsx          # Carte draggable
│   │   ├── KanbanFilters.tsx       # Filtres Kanban
│   │   ├── KanbanMetrics.tsx       # Métriques en-tête
│   │   └── index.ts                # Exports
│   ├── taches/
│   │   ├── TacheKanbanCard.tsx     # Carte tâche spécifique
│   │   ├── TacheQuickEdit.tsx      # Modal édition rapide
│   │   ├── TacheBulkActions.tsx    # Actions multiples
│   │   └── index.ts
│   ├── opportunites/
│   │   ├── OpportuniteKanbanCard.tsx
│   │   ├── OpportuniteQuickEdit.tsx
│   │   └── index.ts
│   └── metrics/
│       ├── MetricCard.tsx
│       ├── TrendIndicator.tsx
│       ├── SparklineChart.tsx
│       └── index.ts
└── dashboard/
    ├── taches/
    │   └── page.tsx                # Utilise KanbanBoard
    ├── opportunites/
    │   └── page.tsx                # Utilise KanbanBoard
    └── metrics/
        └── page.tsx                # Dashboard consolidé (optionnel)
```

### Interfaces TypeScript

```typescript
// Kanban générique
interface KanbanColumn<T> {
  id: string
  title: string
  color: string
  items: T[]
  count: number
  metadata?: Record<string, any> // Ex: valeur totale pour opportunités
}

interface KanbanBoardProps<T> {
  columns: KanbanColumn<T>[]
  onCardMove: (cardId: string, fromColumn: string, toColumn: string) => Promise<void>
  onCardClick: (card: T) => void
  onCardEdit: (card: T) => void
  onCardDelete: (cardId: string) => Promise<void>
  renderCard: (card: T, isDragging: boolean) => React.ReactNode
  isLoading?: boolean
  emptyMessage?: string
}

// Tâche Kanban
interface TacheKanban {
  id: string
  title: string
  description?: string
  type: TacheType
  priority: TachePriority
  status: TacheStatus
  dueDate?: Date
  assignedTo: {
    id: string
    firstName: string
    lastName: string
    avatar?: string
  }
  client?: {
    id: string
    name: string
  }
  isOverdue: boolean
  createdAt: Date
}

// Opportunité Kanban
interface OpportuniteKanban {
  id: string
  name: string
  description?: string
  type: OpportuniteType
  priority: OpportunitePriority
  status: OpportuniteStatus
  estimatedValue?: number
  confidence?: number
  client: {
    id: string
    firstName: string
    lastName: string
  }
  conseiller: {
    id: string
    firstName: string
    lastName: string
  }
  detectedAt: Date
}
```

---

## 🎨 Design System Kanban

### Colonnes Tâches

```typescript
const TACHE_COLUMNS = [
  {
    id: 'TODO',
    title: 'À faire',
    color: 'bg-slate-100 border-slate-300',
    headerColor: 'bg-slate-500 text-white',
  },
  {
    id: 'IN_PROGRESS',
    title: 'En cours',
    color: 'bg-blue-50 border-blue-200',
    headerColor: 'bg-blue-600 text-white',
  },
  {
    id: 'COMPLETED',
    title: 'Terminé',
    color: 'bg-green-50 border-green-200',
    headerColor: 'bg-green-600 text-white',
  },
]
```

### Couleurs Priorités

```typescript
const PRIORITY_COLORS = {
  LOW: 'bg-gray-100 text-gray-700 border-gray-300',
  MEDIUM: 'bg-blue-100 text-blue-700 border-blue-300',
  HIGH: 'bg-orange-100 text-orange-700 border-orange-300',
  URGENT: 'bg-red-100 text-red-700 border-red-300',
}
```

### Animations

```css
/* Smooth drag */
.kanban-card {
  transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
}

.kanban-card.dragging {
  opacity: 0.5;
  transform: scale(1.05) rotate(2deg);
  box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
}

.kanban-column.drag-over {
  background-color: rgba(59, 130, 246, 0.1);
  border-color: rgb(59, 130, 246);
}
```

---

## ✅ Checklist Implémentation

### Phase 1 : Kanban Tâches (Estimé 6-8h)

- [ ] Installer `@dnd-kit` packages
- [ ] Créer `KanbanBoard.tsx` générique réutilisable
- [ ] Créer `KanbanColumn.tsx` avec drop zone
- [ ] Créer `KanbanCard.tsx` draggable générique
- [ ] Créer `TacheKanbanCard.tsx` avec design spécifique
- [ ] Intégrer dans `dashboard/taches/page.tsx`
- [ ] Implémenter drag & drop avec API PATCH
- [ ] Ajouter filtres (assigné, priorité, type, date)
- [ ] Ajouter métriques tâches (4 cartes stats)
- [ ] Ajouter bulk actions (réassigner, changer priorité)
- [ ] Tests manuels exhaustifs

### Phase 2 : Kanban Opportunités (Estimé 4-6h)

- [ ] Créer `OpportuniteKanbanCard.tsx`
- [ ] Modifier `dashboard/opportunites/page.tsx` pour drag & drop
- [ ] Implémenter changement statut via drag
- [ ] Ajouter métriques opportunités avancées
- [ ] Créer route `/api/advisor/opportunites/metrics`
- [ ] Intégrer graphiques `recharts`
- [ ] Tests manuels

### Phase 3 : Métriques Consolidées (Estimé 4h)

- [ ] Installer `recharts`
- [ ] Créer `MetricCard.tsx` réutilisable
- [ ] Créer `TrendIndicator.tsx`
- [ ] Créer dashboard métriques (optionnel page dédiée)
- [ ] Graphiques : ligne pipe, barres tâches, donut opportunités
- [ ] Tests

### Phase 4 : Objectifs & Projets (Estimé 3h + Différé F5.1)

- [ ] Créer modèle `Objectif` Prisma (si demandé)
- [ ] Service & routes objectifs
- [ ] Widget progression objectifs
- [ ] **Différer** : Lien projets ↔ facturation à F5.1

---

## 📊 Estimation Temps Total F4.3

- **Phase 1 Kanban Tâches** : 6-8h
- **Phase 2 Kanban Opportunités** : 4-6h
- **Phase 3 Métriques** : 4h
- **Phase 4 Objectifs** : 3h (ou différé)
- **Tests intégration** : 2h
- **Documentation** : 1h

**Total** : 17-21h (sans Phase 4) ou 20-24h (avec Phase 4)

---

## 🎯 Critère Validation F4.3

> "L'équipe peut suivre son pipe commercial et ses tâches sans outils externes."

**Validation** :
- ✅ Kanban tâches drag & drop fonctionnel
- ✅ Kanban opportunités drag & drop fonctionnel
- ✅ Métriques en temps réel (tâches en retard, valeur pipe)
- ✅ Filtres et recherche performants
- ✅ Bulk actions pour productivité
- ✅ Pas besoin Trello/Asana/Pipedrive externe

---

## 🚀 Prochaine Action

**Option 1 : Commencer Phase 1 immédiatement** (Kanban Tâches)
- Installer @dnd-kit
- Créer composants Kanban génériques
- Intégrer dans page tâches

**Option 2 : Finaliser F4.2 d'abord** (Migration DB)
- Exécuter `npx prisma db push`
- Tests manuels récurrences
- Puis F4.3

**Recommandation** : **Option 1** car F4.2 code est à 100%, migration DB peut être faite en parallèle par l'utilisateur.

---

**Date création** : 24 novembre 2024, 00h32  
**Prêt pour** : Implémentation Phase 1 Kanban Tâches  
**Rigueur** : Plan détaillé sans simplification, architecture complète, estimations réalistes
