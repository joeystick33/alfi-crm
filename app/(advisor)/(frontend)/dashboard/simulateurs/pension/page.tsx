import { PensionEstimator } from '@/app/(advisor)/(frontend)/components/simulateurs';

export default function PensionEstimatorPage() {
  return (
    <div className="p-6">
      <PensionEstimator />
    </div>
  );
}
