import { TaxStrategyComparison } from '@/app/(advisor)/(frontend)/components/simulateurs';

export default function TaxStrategyComparisonPage() {
  return (
    <div className="p-6">
      <TaxStrategyComparison />
    </div>
  );
}
