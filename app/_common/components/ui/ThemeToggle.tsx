'use client';

/**
 * ThemeToggle - Désactivé car le CRM utilise uniquement le thème light
 * Ce composant est conservé pour éviter de casser les imports existants
 */

const ThemeToggle = () => {
  // Thème light uniquement - pas de toggle
  return null;
};

export default ThemeToggle;
