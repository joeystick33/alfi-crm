server:
  port: 8081
  max-http-request-header-size: 64KB
  tomcat:
    max-http-form-post-size: 10MB
    max-swallow-size: 10MB
spring:
  application:
    name: simulateur-patrimoine-api

# PDF generation microservice
pdf:
  service:
    url: ${PDF_SERVICE_URL:http://localhost:3001}